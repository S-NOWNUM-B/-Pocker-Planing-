export function CreateRoomPage() {
  return (
    <div>
      <h1>Create Room</h1>
      {/* TODO: Move form from current HomePage here — room name, user name, deck type selector */}
    </div>
  );
}
